class Solution {
    public boolean canJump(int[] nums) {
        int lastElementPosition=nums.length-1;

        for(int i=nums.length-2;i>=0;i--){
            if(i+nums[i]>=lastElementPosition){
                lastElementPosition=i;
            }
        }
        return lastElementPosition==0;
        
    }
}
