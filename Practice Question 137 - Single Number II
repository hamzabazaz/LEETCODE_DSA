class Solution {
    public int singleNumber(int[] nums) {
       int once=0;
       int twos=0;
       for(int num:nums){
        once=(once^num)&~twos;
        twos=(twos^num)&~once;
       }
       return once;
    }
}
